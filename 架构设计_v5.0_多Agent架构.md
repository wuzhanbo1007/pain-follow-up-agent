# 架构设计 v5.0 —— 多 Agent 随访架构

> 本文档基于 2026-07-28 对 `pain-followup-demo` 的实际代码改造，取代此前所有架构文档中与代码不符的部分。
> 设计模式参考：virtual-hospital-agents 项目的「编排器 + Graph-per-Agent + 结构化输出 + 规则兜底」。

## 1. 改造背景

改造前项目只有 **1 个真正意义上的 Agent**（C 号执行 Agent 的 ReACT 循环），其余模块存在以下问题：

| 问题 | 位置 |
|------|------|
| 当日随访判定为纯硬编码规则 | `engine/followup_scheduler.py` |
| 审阅模块无任何 LLM 能力，仅是数据管道 | `agents/review.py` |
| LLM client 在 6+ 处各自 `OpenAI(...)`，无统一封装 | `llm/`、`agents/`、`engine/` |
| ReACT 循环存在两套重复实现（REST / WebSocket） | `agents/execution.py` vs `services/followup_service.py` |
| `followup_service` 绕过封装直接访问 `message_generator.client` 私有属性 | `services/followup_service.py` |

## 2. 目标架构

```
                    ┌──────────────────────────────────┐
                    │   orchestrator.py（编排器·新增）    │
                    │   ctx 字典串接，Agent 互不直调       │
                    └──────┬──────┬──────┬──────┬───────┘
                           │      │      │      │
        ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐
        │ A 计划Agent│ │ B 判定Agent│ │ C 执行Agent│ │ D 审阅Agent│
        │ planner   │ │ scheduler │ │ execution │ │ review    │
        │ (已有,LangGraph)│ │ (新增,规则+LLM)│ │ (已有,ReACT) │ │ (新增,结构化) │
        └──────────┘ └──────────┘ └──────────┘ └──────────┘
                           │      │      │      │
        ┌──────────────────┴──────┴──────┴──────┴───────┐
        │ 共享底座：llm/client.py(新增·唯一LLM入口)         │
        │ prompts/(保留) · knowledge/RAG(保留) · SQLite   │
        └────────────────────────────────────────────────┘
```

### 核心设计原则（源自参考项目）

1. **每个 Agent 一个模块 + 入口函数**：`decide_today()` / `run_session()` / `run_review()` / `run_followup_pipeline()` 是各自唯一入口，可插拔、可单测。
2. **编排器串接，Agent 互不直调**：`orchestrator.run_followup_pipeline` 用 `ctx` 字典依次传递 B→C→D 的结果；B 判定"今日无需随访"时提前返回。
3. **结构化输出 + 规则兜底**：所有 LLM 输出为 JSON 并程序化解析（`parse_json_safe`），LLM 不可用或解析失败时一律回退到规则/模板路径 —— 保证 demo 在无网络/无 key 时依然完整可跑。
4. **LLM 不直接写库**：落库字段由代码兜底填充，Agent 只产出结构化建议。

## 3. 四个 Agent 职责

| Agent | 文件 | 职责 | LLM 用法 |
|-------|------|------|---------|
| **A 计划 Agent** | `agents/planner.py` | 随访计划生成（抽取→RAG→草稿→HITL→落库） | LangGraph 工作流（原有） |
| **B 判定 Agent** | `agents/scheduler_agent.py` ★新增 | 当日随访判定（C1~C4）。规则优先（批量快、零成本），规则无法明确判定/频次解析失败时才调 LLM 结构化判定 | 混合：规则 + LLM 兜底，返回 `{decision, reason, source}` |
| **C 执行 Agent** | `agents/execution.py` | 随访对话执行，ReACT 自主循环决定追问轮次 | 反思/追问/解析逻辑已抽入 `react_core.py` 共享 |
| **D 审阅 Agent** | `agents/review_agent.py` ★新增 | 对完成的会话做智能审阅，输出 `{summary, risk_flags, completion_score, suggestions, source}` | LLM 结构化审阅 + 规则兜底，结果写入 review 的 `audit_snapshot.ai_review` |

## 4. 新增/修改文件清单

**新增（7 个）**
- `backend/llm/client.py` —— 统一 LLM 单例：`get_client()` / `chat()` / `is_llm_available()` / `parse_json_safe()`
- `backend/agents/scheduler_agent.py` —— B 号判定 Agent
- `backend/agents/review_agent.py` —— D 号审阅 Agent
- `backend/agents/react_core.py` —— 共享 ReACT 内核（反思决策），REST 与 WebSocket 两条路径共用
- `backend/agents/orchestrator.py` —— 编排器
- `backend/prompts/schedule_decision.py`、`backend/prompts/review_analysis.py` —— 新 Agent 的 prompt（延续 prompt 与代码分离的约定）

**修改（11 个）**
- `llm/message_generator.py`、`llm/parser.py`、`agents/planner.py`、`engine/auto_reply.py` —— 全部改用 `llm/client.py` 单例
- `agents/execution.py` —— ReACT 逻辑迁入 `react_core`；顺带修复了无 LLM 时 ReACT 轮次不自增导致的死循环隐患
- `agents/state.py` —— 补声明 `react_round` 等状态通道
- `services/followup_service.py` —— 删除私有属性访问与重复 ReACT，改调 `react_core.reflect_decision`；会话结束后自动触发 D 号审阅
- `app.py` —— `/api/followup/today` 改走 B 号 Agent 的 `decide_today_list`（前端返回结构不变，附加 `reason/source` 字段）
- `routes/review.py` —— `POST /api/sessions/run/{pid}` 改走编排器（返回向前兼容）
- `agents/__init__.py` —— 统一导出 + 职责注释
- `mock_data/followup_db.py` —— 新增 `get_review_by_session` / `attach_ai_review`（不改表结构）

## 5. 数据流（一次完整随访）

```
GET /api/followup/today
  └─ B 判定 Agent（规则→必要时 LLM）→ 今日应随访名单

POST /api/sessions/run/{pid}
  └─ orchestrator.run_followup_pipeline(pid)
       ├─ B decide_today       → need_followup? 否则提前返回
       ├─ C run_session        → ReACT 对话，产出 session + transcripts
       ├─ D run_review         → 结构化审阅 JSON
       └─ 落库                 → review.audit_snapshot.ai_review
```

WebSocket 演示路径（`demo:start_followup`）不再自带一套 ReACT，改用 `react_core` 同一实现，事件推送副作用保留在 `followup_service`。

## 6. 收尾完善（2026-07-28 下午已完成）

1. ~~统一嵌入通道~~ ✅ `llm/client.py` 新增 `get_embedding_client()`（按凭据缓存单例），`knowledge/embeddings.py` 的两个 Provider 均改用它；嵌入配置（`EMBEDDING_*`）与 chat 配置（`LLM_*`）保持独立。
2. ~~tools.py 死代码~~ ✅ 经全局搜索确认 `ToolDispatcher` 被 `core/container.py` 真实引用装配进 `AppContainer.dispatcher`，**并非死代码**，已在文件 docstring 标注引用关系。
3. ~~app.py 瘦身~~ ✅ 483 行 → 97 行。新增 `core/runtime.py`（共享状态）、`core/bootstrap.py`（启动装配）、`routes/patients.py`（REST）、`routes/ws.py`（`register_ws_events`），app.py 只保留装配与入口。
4. ~~端到端验证~~ ✅ 已以 `LLM_API_KEY=""` 规则兜底模式启动服务实测：`/api/patients`(50人)、`/api/followup/today`(send=21/skip=29，走B号)、`/api/stats`、`POST /api/sessions/run/{pid}`（编排器 B→C→D 全链路 200）、`/api/knowledge/status`(414 条知识) 全部通过。

## 7. 后续方向

1. WebSocket 事件（`demo:*`）逻辑已迁移但未用 socket 客户端实测，建议接前端联调一次确认推送正常。
2. 真实 LLM 链路（B 兜底判定 / C ReACT 完整轮次）尚未在真实模型上压测（避免调用费用）；D 号 LLM 路径已实测可返回合法 JSON。
3. 若后续要贴近参考项目形态，可把 B/D 升级为 LangGraph `build_*_graph()` 工厂（当前为轻量函数式 Agent，接口已对齐，升级成本低）。
4. 完全离线环境运行时，可同时置空 `EMBEDDING_API_KEY`，确保 RAG 检索也零外部调用。
