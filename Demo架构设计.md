# 慢性疼痛随访智能体 — Demo 架构设计

> 版本：v1.0
> 日期：2026-07-03
> 目标：支撑 30 分钟演讲演示，展示智能体核心能力（自主触发 / 工具调用 / 条件重规划）

---

## 一、设计原则

| 原则 | 说明 |
|------|------|
| **演示优先** | 架构为演示服务，不追求生产级完整性 |
| **真实感** | 数据和交互要像真实系统，不用占位符 |
| **可解释性** | 智能体的每一步决策都可视化，让观众看懂"它在想什么" |
| **可回退** | 演示过程中任何步骤都能跳过或重来，防止现场翻车 |
| **模块化** | 各组件解耦，方便后续扩展其他智能体 Demo |

---

## 二、整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                      演示前端层                              │
│  ┌──────────┐  ┌──────────────┐  ┌─────────────────────┐ │
│  │ 微信界面  │  │ 医生管理后台  │  │ 智能体决策可视化      │ │
│  │ (患者端)  │  │ (Doctor DAO) │  │ (推理链实时展示)     │ │
│  └──────────┘  └──────────────┘  └─────────────────────┘ │
└──────────────────────┬────────────────────────────────────┘
                       │ HTTP / WebSocket
┌──────────────────────▼────────────────────────────────────┐
│                   智能体编排层                               │
│  ┌─────────────────────────────────────────────────────┐  │
│  │              Agent Orchestrator                      │  │
│  │  • 任务规划 (ReAct / Plan-and-Execute)             │  │
│  │  • 工具调用调度 (Tool Dispatcher)                   │  │
│  │  • 异常处理与回溯 (Re-planning)                     │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                             │
│  工具集 (Tool Set):                                        │
│  ┌─────────┐ ┌─────────┐ ┌────────┐ ┌──────────────┐   │
│  │微信消息  │ │风险评分  │ │预警推送│ │病历结构化    │   │
│  │发送工具  │ │工具     │ │工具    │ │工具          │   │
│  └─────────┘ └─────────┘ └────────┘ └──────────────┘   │
└──────────────────────┬────────────────────────────────────┘
                       │
┌──────────────────────▼────────────────────────────────────┐
│                     模型与数据层                             │
│  ┌──────────┐  ┌──────────────┐  ┌──────────────────┐  │
│  │  LLM     │  │  患者数据库   │  │  规则与知识库     │  │
│  │ (推理核心)│  │  (Mock数据)  │  │  (风险评分规则)   │  │
│  └──────────┘  └──────────────┘  └──────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

---

## 三、核心模块设计

### 3.1 智能体编排层（Agent Orchestrator）

这是整个 Demo 的灵魂，负责让智能体"活起来"。

#### 状态机设计

```python
class FollowUpAgentState(Enum):
    IDLE           = "空闲，等待触发"
    FETCHING       = "拉取今日随访名单"
    SENDING        = "向患者发送随访消息"
    WAITING_REPLY = "等待患者回复"
    PARSING        = "解析患者自然语言回复"
    SCORING        = "计算风险评分"
    ROUTING        = "风险分层路由"
    ALERTING       = "向医生推送预警"
    LOGGING        = "记录随访结果"
    ERROR          = "异常处理"
```

#### 触发方式

| 触发类型 | 说明 | Demo 中的模拟方式 |
|---------|------|------------------|
| 定时触发 | 每日 09:00 自动开始随访 | 演示时点击"开始今日随访"按钮模拟 |
| 事件触发 | 患者回复消息时立即解析 | WebSocket 实时推送 |
| 异常触发 | 连续3天无回复自动升级 | 演示中手动触发该分支 |

#### 工具调用框架（简化版）

```python
TOOL_REGISTRY = {
    "send_wechat_message": {
        "description": "向指定患者发送微信随访消息",
        "params": ["patient_id", "message"],
        "mock": True   # Demo 中不发真实微信，用界面模拟
    },
    "parse_natural_reply": {
        "description": "用 LLM 解析患者自然语言回复，提取疼痛评分/NRS/睡眠/用药等信息",
        "params": ["reply_text"],
        "llm_call": True
    },
    "calculate_risk_score": {
        "description": "基于患者历史 + 今日回复计算风险评分",
        "params": ["patient_id", "today_data"],
        "risk_rules": "references/risk_scoring_rules.json"
    },
    "push_doctor_alert": {
        "description": "向主管医生推送预警消息",
        "params": ["doctor_id", "patient_id", "risk_level", "reason"],
        "mock": True
    },
    "query_patient_history": {
        "description": "查询患者历史随访数据",
        "params": ["patient_id", "days"],
        "data_source": "mock_patient_db"
    }
}
```

---

### 3.2 数据层设计

#### 患者数据模型（Mock）

```json
{
  "patient_id": "P20240001",
  "name": "张XX",
  "age": 58,
  "diagnosis": "带状疱疹后神经痛",
  "discharge_date": "2026-03-15",
  "doctor_id": "D001",
  "doctor_name": "李医生",
  "follow_up_plan": {
    "frequency": "每日",
    "duration_days": 90,
    "pain_type": "神经病理性"
  },
  "history": [
    {
      "date": "2026-07-01",
      "nrs_score": 4,
      "sleep_quality": "一般",
      "medication_taken": true,
      "reply_text": "今天还好，大概4分吧，睡觉还是有点受影响"
    }
  ]
}
```

#### 风险评分规则（核心算法）

```python
# 风险评分 = 基础分 + 动态分
# 基础分：基于诊断类型、年龄、合并症
# 动态分：基于近期随访数据趋势

RISK_LEVELS = {
    "low":    {"score_range": [0, 3],   "action": "无需干预，继续常规随访"},
    "medium": {"score_range": [4, 6],   "action": "3日内关注，调整用药建议"},
    "high":   {"score_range": [7, 10],  "action": "立即预警医生，建议复诊"}
}

# 动态分计算规则
def calc_dynamic_risk(today_data, history_7d):
    score = 0
    # NRS 评分趋势
    if today_data["nrs"] >= 7:
        score += 3
    elif today_data["nrs"] >= 5:
        score += 1
    
    # 连续恶化趋势（3日 NRS 持续上升）
    if is_rising_trend([h["nrs"] for h in history_7d[-3:]]):
        score += 2
    
    # 用药不规律
    if not today_data.get("medication_taken"):
        score += 1
    
    # 睡眠持续差
    if today_data.get("sleep_quality") in ["差", "很差"]:
        recent_sleep = [h.get("sleep_quality") for h in history_7d[-3:]]
        if all(s in ["差", "很差"] for s in recent_sleep):
            score += 1
    
    return score
```

---

### 3.3 前端层设计

#### 三个界面

**界面一：患者微信端（模拟手机界面）**

```
┌─────────────────────────────────┐
│  ◀  微信              •••       │
├─────────────────────────────────┤
│  疼痛随访助手                  │
│  对方正在输入...               │
│                                 │
│  您好，张女士！今天是您出院    │
│  第12天，想了解一下您的恢复    │
│  情况 😊                       │
│                                 │
│  1️⃣ 今天疼痛几分？（0-10分） │
│  2️⃣ 昨晚睡眠怎么样？          │
│  3️⃣ 今天的药都有按时吃吗？    │
│                                 │
│  ┌───────────────────────────┐ │
│  │  [输入回复...        ] 🎤 │ │
│  └───────────────────────────┘ │
└─────────────────────────────────┘
```

**界面二：医生管理后台（暗色大屏风格）**

```
┌──────────────────────────────────────────────────────┐
│  PainSmart 疼痛智能随访管理平台      [开始今日随访]  │
├──────────────────────────────────────────────────────┤
│  今日随访概览                          2026-07-03   │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ │
│  │ 应随访   │ │ 已随访   │ │ 高风险   │ │ 中风险   │ │
│  │  200    │ │  186    │ │   3     │ │  27     │ │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘ │
│                                                    
│  患者风险分布                     [实时刷新 ▼]       │
│  ┌──────────────────────────────────────────────┐  │
│  │ 🔴 高风险  张XX  P20240001  NRS:7 趋势↑    │  │
│  │ 🔴 高风险  李XX  P20240015  NRS:8 未用药    │  │
│  │ 🟡 中风险  王XX  P20240008  NRS:5 睡眠差    │  │
│  │ 🟢 低风险  赵XX  P20240023  NRS:2 稳定      │  │
│  │ ...                                           │  │
│  └──────────────────────────────────────────────┘  │
│                                                     │
│  💡 智能体决策日志                                 │
│  [09:00] 触发今日随访，共200人                     │
│  [09:03] 发送随访消息 186 人（14人免随访）         │
│  [09:15] 收到张XX回复，解析中...                   │
│  [09:15] 张XX NRS=7，风险评分=高，已推送李医生    │
└──────────────────────────────────────────────────────┘
```

**界面三：智能体决策可视化（演讲核心）**

```
┌──────────────────────────────────────────────────────┐
│           智能体正在思考...                          │
├──────────────────────────────────────────────────────┤
│                                                     │
│  🤔 当前任务：处理患者张XX的今日随访回复             │
│                                                     │
│  Step 1: [✓] 读取患者回复                          │
│          "今天差不多还是5分，睡觉不好"               │
│                                                     │
│  Step 2: [✓] 调用 parse_natural_reply              │
│          → NRS: 5, 睡眠: 差, 用药: 未知            │
│                                                     │
│  Step 3: [✓] 查询历史数据 (query_patient_history)  │
│          → 近7天 NRS: [4,5,5,6,5,5,5]             │
│                                                     │
│  Step 4: [✓] 计算风险评分 (calculate_risk_score)   │
│          → 总分: 5 (中风险)                        │
│          → 原因: NRS≥5 且睡眠持续差                 │
│                                                     │
│  Step 5: [→] 路由决策...                           │
│          → 中风险 → 无需立即预警                    │
│          → 行动: 记录结果，明日继续关注              │
│                                                     │
│  [查看详细推理链]  [跳过此患者]                     │
└──────────────────────────────────────────────────────┘
```

---

## 四、Demo 数据流设计

### 完整随访流程（一次完整的智能体执行）

```
[定时触发/手动点击]
        │
        ▼
  ┌─────────────┐
  │ 拉取今日    │
  │ 随访名单    │
  └──────┬──────┘
         │ (200人)
         ▼
  ┌─────────────┐
  │ 批量发送    │
  │ 随访消息    │──────→ [微信界面模拟发送动画]
  └──────┬──────┘
         │
         ▼
  ┌─────────────┐◄─── [患者回复]
  │ 监听回复    │
  │ (WebSocket) │
  └──────┬──────┘
         │ (收到张XX回复)
         ▼
  ┌─────────────┐
  │ 解析回复    │───→ LLM调用 (extract NRS, sleep, med)
  └──────┬──────┘
         │
         ▼
  ┌─────────────┐
  │ 查询历史    │───→ Mock DB 查询近7天数据
  └──────┬──────┘
         │
         ▼
  ┌─────────────┐
  │ 风险评分    │───→ 规则引擎计算
  └──────┬──────┘
         │ (高风险)
         ▼
  ┌─────────────┐
  │ 路由决策    │───→ 判断是否需要预警医生
  └──────┬──────┘
         │ (需要预警)
         ▼
  ┌─────────────┐
  │ 推送预警    │───→ [医生端界面弹出预警]
  └─────────────┘
```

---

## 五、技术选型建议

| 层级 | 推荐技术 | 理由 |
|------|---------|------|
| 前端 | **React + Tailwind** | 快速构建，Tailwind 适合做暗色/亮色双主题 |
| 智能体框架 | **LangGraph（Python）** | 状态机清晰，支持条件边和回溯，演示逻辑表达最准确 |
| LLM | **GPT-4o / Claude 3.5** | 解析自然语言回复效果好 |
| 实时通信 | **WebSocket** | 患者回复 → 医生端实时更新 |
| 数据存储 | **SQLite（Mock）** | Demo 无需真实数据库，SQLite 文件即数据 |
| 演示控制 | **Python Flask** | 轻量后端，串联各模块 |

### 备选方案（更简单）

如果希望 Demo 搭建更快，可以用**纯前端模拟智能体行为**：

```
前端 (React) 
  ├── Mock Agent State Machine (JavaScript)
  ├── Mock LLM 回复（预置回复库）
  ├── Mock 数据库（JSON 文件）
  └── 演示控制面板（手动触发各步骤）
```

优点：无需后端，部署极简，演讲时打开浏览器即可
缺点：智能体行为不够"真实"，LLM 调用是假的

---

## 六、Demo 演讲时的交互设计

### 场景一：智能体自主运行（5分钟）

```
演讲者： "现在时间是早上9点，智能体自动开始今日随访"
  → 点击 [开始今日随访]
  → 大屏幕展示：名单拉取 → 消息发送 → 进度条

演讲者： "200个患者，3分钟内全部完成消息发送，
          接下来智能体进入等待回复状态"
  → 展示智能体状态：WAITING_REPLY（等待动画）
```

### 场景二：患者回复触发解析（5分钟）

```
演讲者： "看，张女士回复了"
  → 微信界面弹出回复气泡
  → 智能体状态切换：PARSING
  → [决策可视化界面] 逐步展示推理链

演讲者： "注意这里，智能体不是在简单问答，
          它在自主决定要先查历史、再算风险、再决定要不要预警医生"
  → 展示完整的工具调用序列
```

### 场景三：高风险预警（3分钟）

```
演讲者： "系统判断张女士是高风险，自动推送预警给李医生"
  → 医生端界面弹出红色预警卡片
  → 展示预警内容（含推理依据）

演讲者： "从患者回复到医生收到预警，全程无需人工介入，
          这就是智能体和聊天机器人的本质区别"
```

### 场景四：异常回溯（可选，3分钟）

```
演讲者： "如果患者的回复智能体理解不了怎么办？"
  → 模拟一条模糊回复："还行吧"
  → 智能体调用 parse 失败
  → 自动回溯：发送澄清问题
  → 展示 re-planning 能力
```

---

## 七、文件结构规划

```
pain-followup-demo/
├── backend/
│   ├── agent/
│   │   ├── orchestrator.py       # 智能体状态机
│   │   ├── tools.py              # 工具集实现
│   │   └── risk_engine.py        # 风险评分规则引擎
│   ├── mock_data/
│   │   ├── patients.json         # 200个模拟患者
│   │   ├── replies.json          # 预置回复库
│   │   └── history.db           # SQLite 历史数据
│   ├── llm/
│   │   └── parser.py             # LLM 调用封装（回复解析）
│   └── app.py                    # Flask 主服务
├── frontend/
│   ├── src/
│   │   ├── components/
│   │   │   ├── WechatMock.jsx   # 微信界面模拟
│   │   │   ├── DoctorDashboard.jsx # 医生管理后台
│   │   │   └── AgentThinking.jsx   # 决策可视化
│   │   ├── pages/
│   │   │   └── DemoPage.jsx     # Demo 主页面（三栏布局）
│   │   └── App.jsx
│   └── package.json
├── demo_control/
│   └── scenario_player.py        # 演示场景控制器（自动播放）
└── references/
    └── risk_scoring_rules.json  # 风险评分规则配置
```

---

## 八、下一步行动

| 步骤 | 内容 | 预计时间 |
|------|------|---------|
| 1 | 确认架构设计，选择技术路线 | 今日 |
| 2 | 搭建前端三栏布局（微信端+医生端+决策可视化） | 2天 |
| 3 | 实现智能体状态机（LangGraph 或 JS 状态机） | 2天 |
| 4 | 接入 LLM 解析患者回复 | 1天 |
| 5 | 制作 Mock 数据（200患者 + 回复样本） | 1天 |
| 6 | 联调，完整走通 Demo 流程 | 1天 |
| 7 | 演讲彩排，优化演示节奏 | 1天 |

---

## 附录：为什么这个架构能讲清楚"智能体"？

| 对比维度 | 聊天机器人 | 本架构中的智能体 |
|---------|-----------|----------------|
| 谁触发 | 人问 | **定时/事件自动触发** |
| 能做什么 | 生成文字 | **调用工具、操作数据、推送消息** |
| 一步还是多步 | 单轮 | **多步推理链，有条件分支** |
| 出错怎么办 | 答非所问 | **自动回溯，换路径重试** |
| 管几个人 | 一对一问答 | **一对多，同时管理200人** |

演讲时，架构图中的**每一个箭头**都可以对应一句话解释，让台下医生真正理解智能体不是"会说话的模型"，而是"能替你工作的数字员工"。
